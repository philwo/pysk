# nginx aliases config
# auto-generated by Pysk

{% for a in aliases %}
server {
    listen {{my_ip}}:80;
    server_name {{a.fqdn}} {% if a.www_alias %}www.{{a.fqdn}}{% endif %};
    access_log off;
    rewrite ^/(.*)$ {{a.target}}{% ifnotequal a.target|last "/" %}/{% endifnotequal %}$1 permanent;
}
{% endfor %}
